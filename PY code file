# app.py
from dash import Dash, html, dcc, Input, Output
import plotly.express as px
import pandas as pd
import numpy as np
import os

# If automobile_sales.csv does not exist, create a sample dataset for testing
CSV_PATH = "automobile_sales.csv"
if not os.path.exists(CSV_PATH):
    rng = np.random.default_rng(42)
    years = np.arange(2010, 2024)
    months = np.arange(1, 
